# typescript-eslint peer-declares typescript < 6.1.0 but the actual runtime
# compatibility extends to TypeScript 7. Use legacy peer-deps resolution so
# `npm ci` doesn't reject the install until typescript-eslint ships an updated
# peer range.
legacy-peer-deps=true
